export type State = {
	label: string
	source: string
	startActive: boolean
	stopActive: boolean
}

export type ModalState = {
	info: boolean
	settings: boolean
}

export type BindsState = {
	start: string
	stop: string
}
